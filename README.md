# Night Ride Story — Vercel Final V3

Bản này chỉ cần `index.html` + `vercel.json`.

- Toàn bộ hình ảnh đã được nhúng trực tiếp vào `index.html`, vì vậy **không phụ thuộc thư mục assets** và tránh lỗi không tải được hình trên GitHub/Vercel.
- Tài xế là layer riêng, nhỏ hơn nhẹ so với trước.
- Các bước cùng địa điểm không còn chạy xe qua lại.
- Khi trả khách, tài xế đứng yên; khách cũ fade out, khách mới fade up tại cùng địa điểm.

Upload hai file này vào root repository GitHub rồi deploy với Framework Preset = Other.
